PROJECT CONTEXT:
App: PHYSICS.LAB — JEE/NEET physics learning platform
Stack: React 19, TypeScript, Tailwind CSS 4, Vite 7
Theme: dark, bg-slate-950, primary #38bdf8 (sky-400)
No new npm packages. Functional components only.
Path alias @ = src/

EXISTING PATTERNS TO FOLLOW EXACTLY:
- NumberInput component (blank by default, placeholder shows default value)
- Result box: bg-slate-800 border border-slate-700/50 rounded-lg p-2.5
- Section header: text-xs font-bold uppercase tracking-wider text-slate-400
- Play/Pause button: bg-emerald-500/20 text-emerald-400 border-emerald-500/40
- Canvas: SVG-based, viewBox, dark bg-slate-900, grid pattern
- EnergyBar component for visualizing quantities
- Overall wrapper: flex flex-col bg-slate-950 text-slate-100 p-4 space-y-5 overflow-y-auto min-h-full

CHAPTER TO BUILD:
Name: Rotational Motion
JEE Code: CH-09
Key concepts: [LIST 3-5 CONCEPTS]
Key formulas: [LIST FORMULAS]

GENERATE THESE 5 FILES:

─────────────────────────────────────
FILE 1: src/components/chapters/[folder-name]/Playground.tsx

Requirements:
- SVG-based interactive simulation (NO canvas element)
- requestAnimationFrame animation loop with Play/Pause/Reset
- NumberInput component (exact pattern below) for all parameters
- Real-time physics calculations displayed in Result boxes
- EnergyBar components where relevant
- At least 3 user-controllable parameters
- Simulation must be physically accurate
- Show vector arrows where applicable
- Key insight box at bottom: bg-sky-500/10 border border-sky-500/30

NumberInput pattern to use EXACTLY:
function NumberInput({ label, unit, value, onChange }) {
  const [display, setDisplay] = useState('');
  const handleChange = (e) => {
    const raw = e.target.value;
    setDisplay(raw);
    const parsed = parseFloat(raw);
    if (!isNaN(parsed) && parsed > 0) onChange(parsed);
  };
  const handleBlur = () => {
    const parsed = parseFloat(display);
    if (isNaN(parsed) || parsed <= 0) setDisplay('');
  };
  return (
    <div>
      <label className="block text-[10px] font-bold text-slate-400 uppercase mb-1">{label}</label>
      <div className="flex items-center bg-slate-800 rounded border border-slate-700 focus-within:border-sky-500 focus-within:ring-1 focus-within:ring-sky-500/50 transition-colors">
        <input type="number" value={display} placeholder={String(value)}
          onChange={handleChange} onBlur={handleBlur}
          className="w-full bg-transparent px-3 py-2 text-sm font-mono text-slate-100 outline-none placeholder:text-slate-500" />
        {unit && <span className="text-xs text-slate-500 pr-3 select-none font-mono shrink-0">{unit}</span>}
      </div>
    </div>
  );
}

─────────────────────────────────────
FILE 2: src/components/chapters/[folder-name]/Explore.tsx

Requirements:
- Wrapper: space-y-6
- h2 title (text-2xl font-light)
- p description in text-muted-foreground
- glass-panel highlighted box for core idea with border-l-4 border-primary
- Real world examples as bullet list
- 2 example cards in sm:grid-cols-2 grid using bg-secondary/40
- Italic note at bottom
- Dark theme using CSS variables (text-foreground, text-muted-foreground, bg-secondary)

─────────────────────────────────────
FILE 3: src/components/chapters/[folder-name]/Understand.tsx

Requirements:
- Wrapper: space-y-6
- Concept cards: bg-secondary/30 p-4 rounded-lg
- Formula boxes: font-mono text-sm text-primary bg-black/20 p-2 rounded
- Cover ALL key formulas for JEE
- JEE traps box at bottom: bg-primary/10 border border-primary/30 rounded-lg p-4
- At least 3 JEE common mistakes listed

─────────────────────────────────────
FILE 4: src/components/chapters/[folder-name]/practice/questionBank.ts

Requirements:
- Export Difficulty type: "easy" | "jee-main" | "jee-advanced"
- Export Question interface with: id, difficulty, question, options[], answer (0-indexed), solution[]
- Export questionBank array with exactly 3 questions (one per difficulty)
- Questions must be real JEE-style numerical problems
- Solutions must be step-by-step with formulas shown
- All answers must be mathematically verified

─────────────────────────────────────
FILE 5: src/components/chapters/[folder-name]/practice/PracticeTab.tsx

Requirements:
- Difficulty tabs: Easy | JEE Main | JEE Advanced
- Rounded-full pill style tabs
- Active tab: bg-primary/20 text-primary border-primary
- Show one question at a time
- Options as full-width buttons
- Green highlight correct, red highlight wrong after selection
- Show step-by-step solution after answer
- Next button disabled until answer selected
- Resets on tab change

─────────────────────────────────────

IMPORTANT RULES:
1. Return ALL 5 files completely — no placeholders or "// rest of code"
2. Every file must be production ready and bug free
3. Physics calculations must be accurate
4. No external libraries
5. TypeScript throughout
6. Do not add min/max hints below inputs
7. Do not pre-fill inputs with numbers (use placeholder only)
8. SVG animations must use requestAnimationFrame
9. All 5 files in one response

Return each file with a clear header like:
=== FILE 1: src/components/chapters/[folder]/Playground.tsx ===